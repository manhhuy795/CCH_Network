# Chay cac lenh nay tai dau nhac mininet>
nodes
net
testpolicy
isolationflows

# testpolicy se in chi tiet cac nhom:
# - Project isolation
# - Branch isolation
# - Voice
# - Internet services
# - Social block
# - Controlled intersite
# - IT support
# - IT least privilege
# - Internet inbound

h20_01 ping -c 2 h30_01     # expected fail
h20_01 ping -c 2 h90        # expected pass
h20_01 ping -c 2 hzalo      # expected pass via Firewall HQ
h20_01 ping -c 2 hcall      # expected pass via Firewall HQ
h20_01 ping -c 2 hsocial    # expected fail at HQ SDN Edge
h50_01 ping -c 2 hcall      # expected pass via Firewall Branch
h50_01 ping -c 2 hsocial    # expected fail
h50_01 ping -c 2 h20_01     # expected fail; only IT can support users across sites
h50_01 ping -c 2 h60_01     # expected fail
h70_01 ping -c 2 h20_01     # expected pass, IT remote support Project A
h70_01 ping -c 2 h50_01     # expected pass, IT remote support Branch via MPLS
h70_01 ping -c 2 hcall      # expected pass, IT can test declared Call App
h70_01 ping -c 2 hsocial    # expected fail, IT is not full-access to Social Media

# Internet/service outside initiating traffic into inside users must fail.
hinternet ping -c 2 h20_01  # expected fail
hzalo ping -c 2 h30_01      # expected fail
hcall ping -c 2 h50_01      # expected fail
hinternet ping -c 2 h70_01  # expected fail

# Real OpenFlow inspection.
sh ovs-ofctl -O OpenFlow13 dump-flows access_hq_a
sh ovs-ofctl -O OpenFlow13 dump-flows core_hq
sh ovs-ofctl -O OpenFlow13 dump-flows dist_branch

# From Ubuntu terminal outside Mininet.
# sudo python3 sdn_mpls_demo/metrics_collector.py ping --source h20_01 --destination h90
# sudo python3 sdn_mpls_demo/metrics_collector.py tcp --source h20_01 --destination hcall --seconds 5
# sudo python3 sdn_mpls_demo/metrics_collector.py udp --source h50_01 --destination hcall --seconds 5
