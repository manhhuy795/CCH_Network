[project]
name = "cch-enterprise-full-sdn-lab"
version = "0.1.0"
description = "Enterprise Full-SDN research and simulation lab"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
